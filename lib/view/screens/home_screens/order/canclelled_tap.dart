import 'package:flutter/material.dart';

class CancelledTap extends StatelessWidget {
  const CancelledTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
